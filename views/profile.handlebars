<div class="container">
    <h3>Profile</h3>
    <div class="row">
        <div class="col s12 m4">
            <div class="card">
                <div class="card-image">
                    <img src="{{user.image}}">
                </div>
                <div class="card-content">
                    <p>{{user.fullname}}</p>
                </div>
            </div>
        </div>
    </div>
</div>